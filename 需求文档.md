# MokioClaw Stage1 · Day1 需求文档

> 日期：2026-08-12 ｜ 来源：AI工程师之路 v3.0 ｜ 环境：mokioclaw（MokioAgent 参考项目）已跑通

## 一、任务定义

本阶段构建一个最小 ReAct Agent（MokioClaw）：

```
用户输入 → Agent 循环（思考 → 动作 → 观察）→ 产出物在工作区
```

保持最简：**单个 actor 节点、5 个工具（FileRead / FileWrite / FileEdit / Grep / Bash）、一个工作区**。

## 二、核心问题：为什么 ReAct 循环比单次 LLM 调用强？

**单次调用的三个根本缺陷：**
1. **信息缺失**：模型是"闭卷考试"——问它"D 盘还剩多少空间"它只能编，幻觉的根源之一
2. **无法验证**：写代码不知道能不能跑，给结论不知道对不对
3. **无法推进**：多步任务（写→跑→改→验）只能"假装"完成，过程不可见、不可改

**ReAct 循环的解法**：Thought（想）→ Action（工具调用）→ Observation（看结果）→ 再想。
- 工具 = 外部世界接口（读文件/执行命令），开卷考试
- Observation = 真实反馈，错了当场知道，能自我纠错
- 循环 = 每步小决策，迭代逼近正确

**边界**：不是所有任务都需要循环。确定路径的任务（翻译、格式化）单次调用更快更省；**任务路径不确定、需要动态决策 + 外部交互**时才值得循环（workflow vs agent 之分）。

> 一句话：**单次调用是让模型"闭卷凭记忆作答"，必然幻觉、不可验证、无法推进；ReAct 通过工具获取真实信息、通过观察自我纠错、通过迭代逼近正确——语言提供推理，工具提供行动，观察提供反馈，三者闭环，模型才从"聊天机器"变成"能干活的人"。**

## 三、实测证据：贪吃蛇任务的运行解读

用参考项目跑任务：「帮我创建一个简易的贪吃蛇游戏代码，并执行出来给我看」，观察到的完整调用链：

```
任务输入
 └─ Intent Router：判定 workflow，confidence 1.0
 └─ 创建工作区（workspace-时间戳/，隔离边界）
 └─ Memory Snapshot（rules / working_memory / history 三层）
 └─ [planner] TodoWriteTool：拆 2 个 todo + 验收标准 + 验证命令
 └─ [planner] CallCodeAgentTool：委派给 codeAgent
 └─ ⇄ Handoff：planner → codeAgent
 └─ [codeAgent] TodoUpdateTool：todo-1 → in_progress
 └─ [codeAgent] FileWriteTool：写入 snake.html（313 行 / 8638B）
 │    返回结构化结果：ok=True, type=create, lines=313, diff...
 └─ [codeAgent] BashTool ×3：
 │    ① 文件存在？行数？canvas 标签？
 │    ② 提取 <script> JS 做结构检查
 │    ③ 括号平衡等语法验证
 └─ 最终答复
```

**逐环节解读（ReAct 在哪里）：**

| 环节 | 体现了什么 |
|---|---|
| 意图路由 | 决策先行：先判断任务类型，不直接开干 |
| 工作区隔离 | 安全边界：agent 只能动自己的空间，这是单次调用没有的"环境"概念 |
| 写计划含验收标准 | 先想清楚"怎么算完成"再动手——agent 设计的核心习惯 |
| FileWriteTool 返回 diff/lines | Observation 的质量保障：工具返回带证据的结构化结果，不是"写完了"一句空话 |
| BashTool ×3 验证 | **ReAct 精华**：写完不直接交付，跑命令验证；每步结果决定下一步（失败则回去改再验） |

**对照单次调用：**

| 维度 | 单次调用 | 本次运行 |
|---|---|---|
| 写文件 | 只能"说"代码 | 真写进文件（FileWriteTool + diff） |
| 验证 | 无法自检 | 3 次命令验证（存在/结构/语法） |
| 环境 | 无 | 独立工作区 + 隔离 |
| 纠错 | 无 | 验证失败 → 改 → 再验 |

**一个诚实的观察**：agent 没有真正"运行"贪吃蛇（HTML 游戏需要浏览器，环境没有）——它选择了**环境允许的最强验证**（静态检查 313 行代码）。这暴露一个设计原则：**agent 的验证受环境能力约束，优秀设计是用可用手段尽可能验证，而不是假装完成**。这个缺口（无浏览器运行环境）可作为未来扩展点。

## 四、由此沉淀的设计要点（Stage1 实现应遵守）

1. **工具返回结构化结果**（ok / diff / lines 等字段），杜绝"假完成"
2. **验证后再交付**：写完跑检查（文件存在 → 内容结构 → 语法）
3. **工作区隔离**：工具只能在 workspace 内操作（5 个工具的边界检查）
4. **先写验收标准再动手**：每个任务带 acceptance_criteria + verification_commands
5. **循环终止条件**：模型输出纯文本（不再调用工具）即结束
